# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/

# OpenTofu Lambda build artifacts (zipped from lambdas/ at plan/apply time)
opentofu/build/

# Lambda layer contents (rebuilt by layers/*/build.sh; only the recipe is committed)
layers/*/python/
